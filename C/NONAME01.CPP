#include<iostream.h>
#include<conio.h>
void main()
{
 clrscr();
 char s[20];
 cout<<"enter string \n  \t NO spaces allowed";
 for(int i=0;i<(int strlen(const char* s));i++)
 {
  if(s[i]==' ')
  {
   exit(0);
   }
 cin>>s;
 cout<<s;
// int strlen(const char* s);
 getch();
 }